================================================================================
ПЛАН РЕФАКТОРИНГА: mission_6states
Перенос функционала из Jupyter-ноутбуков (scripts/) в библиотеку subspace_conjugacy
================================================================================

Дата составления: 2026-09-14
Последнее обновление: 2026-09-19 (аудит фактического статуса реализации —
см. правки в разделе 0 и находки №1-3 ниже; раздел 0 более не отражает
исходное "пустое" состояние библиотеки на 2026-09-14, а фиксирует то, что
реально проверено в коде на дату аудита)
Назначение документа: использовать каждую фазу/подфазу как промпт для отдельной
сессии разработки. Каждый блок содержит контекст, цели, задачи, API, критерии
приёмки и связи с теорией, ноутбуками и библиотекой.

ПРИОРИТЕТ РЕАЛИЗАЦИИ:
  - Канон (source of truth) — описание алгоритма кластеризации из теории (секция 1).
  - Библиотека subspace_conjugacy должна реализовывать канон.
  - Ноутбуки scripts/ — reference implementation + parity-тесты (могут расходиться с каноном).
  - Legacy-модули для NB4 сохраняются только для воспроизведения ноутбуков.

================================================================================
0. КОНТЕКСТ ПРОЕКТА
================================================================================

Проект реализует метод Fursov (подпространственная сопряжённость) для
классификации МРТ-изображений опухолей мозга.

Классы данных:
  - glioma      (глиома)
  - meningioma  (менингиома)
  - pituitary   (гипофиз)
  - test        (тестовая выборка: 75 изображений, по 25 на класс)

Размер изображений после препроцессинга: 256×256 px.
Размерность вектора признаков: 65536 (256×256, горизонтальный или вертикальный обход).

Количество подклассов (кластеров) на каждый класс опухоли: 8 (n_subclasses=8,
подбирается экспериментально, в ноутбуках зафиксировано 8).
Итого глобальных подклассов при классификации: 24 (8 × 3 класса).
Базис каждого подкласса для классификации: ровно 2 опорных вектора (subclass_factor=2).

Исходные ноутбуки (scripts/):
  1_image_resizer.ipynb
  2_dataset_preparing.ipynb / 2_1_dataset_preparing.ipynb
  3_Fursov_method_make_vectors.ipynb
  4_1_Fursov_vector_processing (for glioma|meningioma|pituitary).ipynb
  5_1_Fursov_reference_vector_processing (for glioma|meningioma|pituitary).ipynb
  5_t_Fursov_reference_vector_processing (for glioma)-Copy1.ipynb  [вариант n=12]
  6_Fursov_new subclasses(for glioma|meningioma|pituitary).ipynb
  7_2_Fursov_fulfilling_new_subclasses (for glioma|meningioma|pituitary).ipynb
  7_2_hf_Fursov_fulfilling_new_subclasses (for glioma|meningioma|pituitary).ipynb
  7_master_Fursov_fulfilling_new_subclasses (dataset).ipynb
  8_Fursov_classification.ipynb

СТАТУС НА 2026-09-19 (аудит фактического кода и pytest-прогона; см. также
находки №1-3 ниже): все фазы 0-8 из раздела 4 УЖЕ РЕАЛИЗОВАНЫ в
subspace_conjugacy/ и покрыты тестами. Формулировки фаз ниже сохранены как
техническое задание/референс архитектуры, а не как список работы "с нуля" —
перед тем как писать новый модуль, проверьте, не существует ли он уже.

Существующая библиотека (subspace_conjugacy/), подтверждено аудитом:
  __init__.py, pyproject.toml, requirements.txt        — есть
  config/dataset.py                                    — DatasetConfig
  preprocessing/{resize,centering,normalization,preprocessor}.py — NB1-2
  features/{vectorization,extraction}.py               — NB3
  core/metrics.py          — conjugate_criterion, cosine_similarity_matrix
  algorithms/global_pair.py, reference_centers.py,
    subclass_seed.py, subclass_growth.py               — канон A.1-A.3, B.1-B.2
  algorithms/fursov_clusterer.py                       — фасад A+B
  algorithms/subclass_export.py                        — экспорт Y_s в CSV
  algorithms/legacy/{per_vector_pairs,notebook_pipeline}.py — NB4-7 parity
  models/base.py           — BaseSubspaceEstimator (sklearn-интерфейс)
  models/clusterer.py      — alias на FursovClusterer (не отдельная реализация)
  models/classifier.py     — SubspaceConjugacyClassifier (теория C, NB8)
  io/persistence.py        — pickle / npz / json для моделей
  io/vectors.py            — CSV ноутбуков (NB3, NB5-7)
  pipeline/{stages,fursov_pipeline}.py                 — orchestrator
  evaluation/metrics.py    — accuracy/confusion_matrix отчёт (NB8)
  utils/validation.py      — валидация X, y, Y, гиперпараметров
  tests/ (theory + notebook_parity + unit) — 410 тестов, покрытие 93%
    (pytest --cov=subspace_conjugacy; демо-блоки if __name__ == "__main__"
    убраны из модулей библиотеки в tests/test_*.py, как требовал п.1 Фазы 0)

ИЗВЕСТНЫЕ НАХОДКИ АУДИТА (не блокируют дальнейшую работу, но важны для
корректных ожиданий от "master"-стратегии и от acceptance-критерия accuracy):

  Находка №1 — growth_strategy="master" в ConjugacyClusterGrowth/
  FursovClusterer/SubspaceConjugacyClassifier — это КАНОН-СОВМЕСТИМАЯ
  АППРОКСИМАЦИЯ идеи NB7 (ratio к среднему), а НЕ побитовая реплика NB7.
  Настоящий NB7 назначает по одному вектору КАЖДОМУ подклассу одновременно
  за один "idea"-шаг; "master" здесь ищет одно глобальное (x*, s*) за
  итерацию, чтобы не нарушать инвариант канона B.2. Подробности —
  algorithms/subclass_growth.py, docstring модуля и _find_argmax_master.
  За побитовым воспроизведением NB7 — algorithms/legacy/notebook_pipeline.py.

  Находка №2 — критерий приёмки Фазы 4 "accuracy >= 0.70 на test75 (±0.02)"
  НЕ проверяется буквально и не может быть проверен: оригинального test75
  (75 изображений, выход бажных macOS-путёвых NB1-NB7) в репозитории нет и
  получить его негде. tests/test_parity/test_classifier_real_data.py вместо
  этого проверяет более слабое, но честное утверждение — accuracy на
  собственном holdout-сплите реальных МРТ (datasets/{class}_centered/*.png)
  значимо выше случайного угадывания (>1/3 для 3 классов). Численную цель
  0.70 воспринимать как исторический ориентир ноутбуков, а не как SLA.

  Находка №3 — реальные артефакты ноутбуков (raw/resized/centered
  изображения, CSV из NB3 и частично NB6-7) лежат в репозитории на уровень
  глубже, чем ожидал DatasetConfig(root="data") по умолчанию: фактически
  data/brain_tumor_mri/glioma_raw, data/brain_tumor_mri/5_all_vectors/...
  tests/conftest.py:default_dataset_config исправлен на
  root="data/brain_tumor_mri". Набор артефактов там всё равно неполный
  (нет pituitary/test векторов и части 8_*_subclasses_vectors.csv для
  meningioma) — находка №2 этим не закрывается.

Все пути в ноутбуках захардкожены под macOS:
  /Users/vladkorsikov/research/dataset_main/...
DatasetConfig (config/dataset.py) уже заменяет их на конфигурируемые пути,
совместимые с Windows/Linux/macOS.

================================================================================
1. THEORY ALIGNMENT — КАНОНИЧЕСКИЙ АЛГОРИТМ КЛАСТЕРИЗАЦИИ
================================================================================

Источник: описание алгоритма кластеризации (теория проекта).
Область применения: эталонные (обучающие) изображения одного класса патологии
(одно множество векторов X shape (M, N) на класс).

----------------------------------------------------------------------
1.1. МАТЕМАТИЧЕСКИЕ ОПРЕДЕЛЕНИЯ
----------------------------------------------------------------------

Косинусный критерий (сходство) для векторов a, b:
  cos(a, b) = (a·b) / (||a|| · ||b||)

Показатель сопряжённости вектора x с подпространством, натянутым на столбцы Y
(N×k):
  R(x, Y) = (xᵀ Y (Yᵀ Y)⁻¹ Yᵀ x) / (xᵀ x)

Реализация в библиотеке:
  - cos(a, b)           → cosine_similarity_matrix() или pairwise из неё
  - R(x, Y)             → conjugate_criterion(x, Y) в core/metrics.py
  - (Yᵀ Y)⁻¹            → compute_gram_inverse(Y) с reg_param (Тихонов)

Диапазон R: [0, 1] (clip в metrics.py).

----------------------------------------------------------------------
1.2. ФАЗА A — ПОИСК ЦЕНТРОВ КЛАСТЕРОВ (n_subclasses центров)
----------------------------------------------------------------------

Выполняется на множестве эталонных векторов одного класса.

ШАГ A.1 — Первая пара центров (2 центра):
  Перебрать ВСЕ возможные пары векторов (i, j), i ≠ j.
  Для каждой пары вычислить cos(v_i, v_j).
  Сохранить пару с МИНИМАЛЬНым значением cos — наиболее «непохожие» изображения.
  Результат: center_indices = [idx1, idx2].

  ⚠ Не путать с NB4: там для КАЖДОГО i ищется argmin_j cos(i,j) → trio_list[M×3].
  Канон — одна глобальная пара.

ШАГ A.2 — Третий центр:
  Y = matrix(center_1, center_2) размерности N×2.
  Для каждого оставшегося x: вычислить R(x, Y).
  Выбрать x с МИНИМАЛЬНым R → третий центр.

ШАГ A.3 — Четвёртый и последующие центры (до n_subclasses):
  Y = matrix(все найденные центры) размерности N×k, k = текущее число центров.
  Для каждого оставшегося x: R(x, Y).
  Выбрать x с МИНИМАЛЬНым R → следующий центр.
  Повторять, пока len(centers) < n_subclasses.

Инвариант: при поиске k-го центра Y строится из (k-1) уже найденных центров
(растущая матрица), а не из фиксированной пары.

----------------------------------------------------------------------
1.3. ФАЗА B — ФОРМИРОВАНИЕ КЛАСТЕРОВ ВОКРУГ ЦЕНТРОВ
----------------------------------------------------------------------

После фазы A: n_subclasses центров (по 1 вектору на кластер).
remaining = все векторы класса \ центры.

ШАГ B.1 — Cosine seed (второй вектор в каждый подкласс):
  Для КАЖДОГО центра c отдельно:
    из remaining выбрать вектор v с МИНИМАЛЬНым cos(v, center_c)
    (наиболее «непохожий» на центр по косинусу — второй элемент пары).
  Результат: n_subclasses пар (center_c, v_c), в каждом подклассе ровно 2 вектора.
  Использованные v удалить из remaining.

  Примечание: в каждом подклассе формируется подпространство Y_s (N×2).

ШАГ B.2 — Conjugacy growth (наполнение кластеров):
  Условие входа: в каждом подклассе уже ≥ 2 вектора (после B.1 выполнено).
  Пока remaining не пуст:
    1. Для каждого x ∈ remaining и каждого подкласса s:
         вычислить R(x, Y_s), где Y_s — текущая базисная матрица (N×k_s).
         На этапе классификации k_s=2; при росте k_s может увеличиваться,
         но для финального базиса классификатора фиксируем k=2 (см. NB6–8).
    2. Найти пару (x*, s*) с МАКСИМАЛЬНым R(x*, Y_{s*}).
    3. Присоединить x* к подклассу s*, обновить Y_{s*}.
    4. Удалить x* из remaining.

  Критично: присоединять СТРОГО ПО ОДНОМУ вектору за итерацию (глобальный argmax
  по всем remaining × всем подклассам). Нельзя bulk-assign множество в один кластер.

----------------------------------------------------------------------
1.4. ФАЗА C — КЛАССИФИКАЦИЯ (применение к тестовым векторам)
----------------------------------------------------------------------

Для каждого класса патологии: n_subclasses базисов Y_{c,s} (N×2).
Глобально: C × n_subclasses подпространств (напр. 3×8=24).

Для тестового x:
  R_{c,s} = R(x, Y_{c,s})
  subclass* = argmax_{c,s} R_{c,s}
  class* = c(subclass*)  [0..7→class0, 8..15→class1, ...]

Опционально (NB8): proportion = R_max / mean(R_others) - 1.

----------------------------------------------------------------------
1.5. МАППИНГ: ТЕОРИЯ → МОДУЛИ БИБЛИОТЕКИ → ФАЗЫ РЕФАКТОРИНГА → НОУТБУКИ
----------------------------------------------------------------------

| Шаг теории              | Модуль (канон)                    | Фаза | Ноутбук      | Legacy / parity      |
|-------------------------|-----------------------------------|------|--------------|----------------------|
| A.1 Глобальная min-cos  | global_pair.py                    | 3.1  | —            | NB4 (trio_list) ⚠    |
| A.2–A.3 Центры min R    | reference_centers.py              | 3.2  | NB5          | —                    |
| B.1 Cosine 2-й вектор   | subclass_seed.py                  | 3.3  | NB6 ~        | —                    |
| B.2 Max R, по 1 вектору | subclass_growth.py                | 3.4  | NB7          | —                    |
| Фасад A+B               | clusterer.py (SubspaceClusterer)  | 3.5  | —            | текущий clusterer ⚠  |
| Экспорт базисов k=2     | subclass_export.py                | 3.6  | NB6–7 CSV    | —                    |
| Классификация           | classifier.py                     | 4    | NB8          | —                    |
| Preprocessing           | preprocessing/*                   | 1    | NB1–2        | —                    |
| Векторизация            | features/*                        | 2    | NB3          | —                    |

Условные обозначения:
  ✅ — прямое соответствие
  ~  — частичное / эвристически близко
  ⚠  — расхождение с каноном; сохранять как legacy для parity

----------------------------------------------------------------------
1.6. СООТВЕТСТВИЕ SubspaceClusterer (текущий код) ТЕОРИИ
----------------------------------------------------------------------

  _select_initial_centers():
    A.1 глобальный argmin cos по всем парам     → ✅ совпадает с теорией
    A.2–A.3 argmin R, Y растёт с центрами       → ✅ совпадает с теорией

  fit_predict() — наполнение кластеров:
    Сразу max R, growing basis                   → ⚠ нет фазы B.1 (cosine seed)
    Один вектор за итерацию (global argmax)      → ✅ совпадает с B.2
    Базис растёт > 2                             → ⚠ для классификации нужен k=2

  Действие при рефакторинге: переписать SubspaceClusterer как фасад
  GlobalMinCosinePairFinder → ReferenceCenterBuilder → CosineSecondVectorAttacher
  → ConjugacyClusterGrowth, с опцией freeze_basis_at=2 для экспорта в classifier.

----------------------------------------------------------------------
1.7. РАСХОЖДЕНИЯ НОУТБУКОВ С КАНОНОМ (parity, не source of truth)
----------------------------------------------------------------------

  NB4: per-vector trio_list ≠ теория A.1 (глобальная пара).
       NB5 берёт initial pair из NB4 вручную (напр. индекс 8) — ещё одно расхождение.
       → Legacy: algorithms/legacy/per_vector_pairs.py (NB4 parity only).

  NB6: «предельно отстоящий» через R/cos — близко к B.1, но отдельный CSV-этап.
  NB7: ratio к среднему (master) — расширение B.2, strategy="master".
  NB8: классификация по 24 базисам k=2 — ✅ совпадает с фазой C.

  Parity-тесты: pytest.mark.notebook_parity — сравнение с CSV ноутбуков.
  Theory-тесты: pytest.mark.theory — проверка канонического алгоритма на synthetic.

================================================================================
2. КЛЮЧЕВЫЕ РАСХОЖДЕНИЯ: ТЕОРИЯ vs НОУТБУКИ vs БИБЛИОТЕКА
================================================================================

2.1. Шаг A.1 (первая пара)
  Теория:     глобальный argmin cos по всем парам (одна пара).
  NB4:        для каждого i — argmin_j cos → trio_list[M×3].
  SubspaceClusterer: глобальный argmin → ✅ теория.
  → Канон: global_pair.py. NB4 → legacy.

2.2. Шаги A.2–A.3 (центры через min R)
  Теория = NB5 = SubspaceClusterer._select_initial_centers (после пары) → ✅

2.3. Фаза B.1 (cosine второй вектор к каждому центру)
  Теория:     явный шаг B.1.
  NB6:        ~похожая логика (farthest vector), но как отдельный pipeline stage.
  SubspaceClusterer: отсутствует → ❌
  → Новый модуль subclass_seed.py.

2.4. Фаза B.2 (max R, sequential)
  Теория = NB7 (частично) = SubspaceClusterer loop → ✅ с оговоркой на B.1.

2.5. Фиксированный базис k=2 для классификации
  Теория (фаза C): Y_s (N×2).
  NB6–8: экспорт 16 векторов = 8 пар.
  SubspaceClusterer: базис не ограничен → нужен freeze/export k=2.

2.6. Staged CSV pipeline vs единый fit()
  Ноутбуки: промежуточные CSV между этапами (удобно для экспериментов).
  Теория: единый алгоритм, stages — implementation detail.
  → FursovPipeline.run_stage() для CSV; SubspaceClusterer.fit() для канона in-memory.

2.7. Preprocessing и vectorization
  Только в NB1–3. Не часть теории кластеризации, но нужны для end-to-end pipeline.

2.8. Математическое ядро
  final_step() из ноутбуков ≡ conjugate_criterion() — ✅
  cos_criterea() из NB4 содержит баг (norm первого вектора) — не воспроизводить.

================================================================================
3. ЦЕЛЕВАЯ СТРУКТУРА ПАКЕТА
================================================================================

subspace_conjugacy/
├── __init__.py
├── pyproject.toml
├── config/
│   └── dataset.py
├── preprocessing/                  # NB1–2, вне теории кластеризации
│   ├── resize.py
│   ├── centering.py
│   └── normalization.py
├── features/                       # NB3
│   ├── vectorization.py
│   └── extraction.py
├── core/
│   └── metrics.py                  # [ЕСТЬ] R(x,Y), cosine — ядро теории
├── algorithms/
│   ├── global_pair.py              # Теория A.1
│   ├── reference_centers.py        # Теория A.2–A.3  (NB5)
│   ├── subclass_seed.py            # Теория B.1       (NB6 ~)
│   ├── subclass_growth.py          # Теория B.2       (NB7 ~)
│   ├── subclass_export.py          # Экспорт Y_s (N×2) для classifier
│   ├── fursov_clusterer.py         # Фасад: A.1→A.3→B.1→B.2
│   └── legacy/
│       ├── per_vector_pairs.py     # NB4 trio_list (parity only)
│       └── notebook_pipeline.py    # NB4→5→6→7 staged CSV (parity only)
├── models/
│   ├── base.py                     # [ЕСТЬ]
│   ├── clusterer.py                # → thin wrapper над fursov_clusterer
│   └── classifier.py               # [ЕСТЬ, РАСШИРИТЬ] — теория C / NB8
├── io/
│   ├── persistence.py              # [ЕСТЬ]
│   └── vectors.py                  # CSV NB3–7
├── pipeline/
│   ├── stages.py
│   └── fursov_pipeline.py
├── evaluation/
│   └── metrics.py
└── utils/
    └── validation.py               # [ЕСТЬ]

tests/
├── conftest.py
├── test_metrics.py
├── test_vectorization.py
├── test_algorithms/
│   ├── test_global_pair.py         # теория A.1
│   ├── test_reference_centers.py   # теория A.2–A.3
│   ├── test_subclass_seed.py       # теория B.1
│   ├── test_subclass_growth.py     # теория B.2
│   └── test_fursov_clusterer.py    # end-to-end канон
├── test_classifier.py
├── test_theory/                    # synthetic, канон
│   └── test_clustering_theory.py
└── test_parity/                    # CSV ноутбуков
    ├── test_nb4_legacy.py
    └── test_glioma_notebook_pipeline.py

================================================================================
4. ПЛАН ПО ФАЗАМ (ПРОМПТЫ)
================================================================================

--------------------------------------------------------------------------------
ФАЗА 0 — ИНФРАСТРУКТУРА ПАКЕТА
Приоритет: P0 | Срок: 1–2 дня | Блокирует: всё остальное
Теория: —
--------------------------------------------------------------------------------

ПРОМПТ ДЛЯ АГЕНТА:
  Настрой инфраструктуру Python-пакета subspace_conjugacy в c:\mission_6states.
  Создай pyproject.toml, requirements.txt, __init__.py с публичным API.
  Добавь config/dataset.py с DatasetConfig (замена hardcoded macOS paths).
  Перенеси блоки if __name__ == "__main__" из модулей в tests/.
  Настрой pytest + conftest.py. Добавь pytest markers: theory, notebook_parity.

ЗАДАЧИ:
  [ ] pyproject.toml (numpy, opencv-python-headless, pillow, scikit-learn)
  [ ] requirements.txt
  [ ] subspace_conjugacy/__init__.py — экспорт FursovClusterer, SubspaceConjugacyClassifier,
      conjugate_criterion, cosine_similarity_matrix
  [ ] config/dataset.py — DatasetConfig (root, classes, n_subclasses=8, subclass_factor=2, paths)
  [ ] tests/conftest.py — small_random_vectors, synthetic_image_256
  [ ] pytest.ini: markers theory, notebook_parity

КРИТЕРИИ ПРИЁМКИ:
  - pip install -e . && import subspace_conjugacy
  - DatasetConfig работает с Windows-путями
  - pytest --collect-only без ошибок

--------------------------------------------------------------------------------
ФАЗА 1 — PREPROCESSING (NB1–2)
Приоритет: P2 | Срок: 2–3 дня | Теория: —
--------------------------------------------------------------------------------

(Без изменений по содержанию — см. предыдущую версию плана.)

ПРОМПТ: preprocessing/ из NB1–2, ImagePreprocessor, DatasetConfig, unit-тests.
Модули: resize.py, normalization.py, centering.py.
СВЯЗЬ: NB1, NB2.

--------------------------------------------------------------------------------
ФАЗА 2 — FEATURE EXTRACTION (NB3)
Приоритет: P0 | Срок: 1–2 дня | Теория: подготовка входа X для фазы A
--------------------------------------------------------------------------------

(Без изменений по содержанию — vectorization + io/vectors.py.)

ПРОМПТ: features/vectorization.py, extraction.py, io/vectors.py.
vectorize_horizontal → flatten (65536,), CSV roundtrip.
СВЯЗЬ: NB3.

--------------------------------------------------------------------------------
ФАЗА 3 — АЛГОРИТМЫ КЛАСТЕРИЗАЦИИ (ТЕОРИЯ A–B + LEGACY NB4–7)
Приоритет: P1 | Срок: 5–6 дней | Зависит от: фаза 0, 2
--------------------------------------------------------------------------------

Общий промпт для фазы 3:
  Реализуй КАНОНИЧЕСКИЙ алгоритм кластеризации (секция 1 THEORY ALIGNMENT)
  в subspace_conjugacy/algorithms/. Используй conjugate_criterion и
  cosine_similarity_matrix из core/metrics.py.
  Legacy-модули для NB4–7 — в algorithms/legacy/ для parity-тестов.
  SubspaceClusterer → фасад FursovClusterer.

--- ФАЗА 3.1: GlobalMinCosinePairFinder (ТЕОРИЯ A.1) ---

ПРОМПТ:
  Реализуй algorithms/global_pair.py — канонический шаг A.1.

АЛГОРИТМ (КАНОН):
  sim = cosine_similarity_matrix(X)  # (M, M)
  mask diagonal → inf
  (i, j) = argmin sim
  return (i, j, sim[i,j])

API:
  class GlobalMinCosinePairFinder:
      def fit(self, X: np.ndarray) -> tuple[int, int, float]: ...
      @property
      def pair_indices_(self) -> tuple[int, int]: ...

КРИТЕРИИ:
  - i != j; sim[i,j] <= sim[k,l] для всех k≠l
  - На synthetic: воспроизводимый результат при fixed seed

LEGACY (parity, НЕ канон):
  algorithms/legacy/per_vector_pairs.py — NB4 trio_list (M×3)
  pytest.mark.notebook_parity

ИСТОЧНИК КАНОНА: теория A.1
ИСТОЧНИК LEGACY: 4_1_Fursov_vector_processing (for *).ipynb

--- ФАЗА 3.2: ReferenceCenterBuilder (ТЕОРИЯ A.2–A.3, NB5) ---

ПРОМПТ:
  Реализуй algorithms/reference_centers.py.

АЛГОРИТМ (КАНОН = NB5):
  Вход: X, initial_pair=(idx1, idx2) из GlobalMinCosinePairFinder
  centers = [idx1, idx2]
  while len(centers) < n_subclasses:
      Y = X[centers].T                    # (N, k), k растёт
      remaining = indices not in centers
      R_values = conjugate_criterion(X[remaining], Y)
      new_center = remaining[argmin(R_values)]
      centers.append(new_center)
  return centers  # length n_subclasses

API:
  class ReferenceCenterBuilder:
      def __init__(self, n_subclasses=8, reg_param=1e-8): ...
      def fit(self, X, initial_pair: tuple[int,int]) -> np.ndarray: ...

КРИТЕРИИ:
  - len(centers_) == n_subclasses
  - Y при каждой итерации включает ВСЕ предыдущие центры
  - notebook_parity: compare с glioma_class_core_vectors.csv (может отличаться
    если NB5 использовал пару из NB4, а не global pair)

ИСТОЧНИК: теория A.2–A.3, NB5

--- ФАЗА 3.3: CosineSecondVectorAttacher (ТЕОРИЯ B.1, NB6 ~) ---

ПРОМПТ:
  Реализуй algorithms/subclass_seed.py — канонический шаг B.1.

АЛГОРИТМ (КАНОН):
  Вход: X, center_indices (n_subclasses,)
  remaining = all indices \ center_indices
  pairs = []
  for c in center_indices:
      sims = cosine_similarity(X[remaining], X[c])  # cos с центром
      v_idx = remaining[argmin(sims)]              # min cos = max «непохожесть»
      pairs.append((c, v_idx))
      remaining.remove(v_idx)
  return pairs  # shape (n_subclasses, 2)

API:
  class CosineSecondVectorAttacher:
      def fit(self, X, center_indices) -> np.ndarray: ...
      # pairs_ shape (S, 2)

КРИТЕРИИ:
  - Каждый центр получает ровно один второй вектор
  - Все 2×S индексов уникальны
  - notebook_parity (~): glioma_new_classes.csv может отличаться

ИСТОЧНИК: теория B.1; NB6 — reference

--- ФАЗА 3.4: ConjugacyClusterGrowth (ТЕОРИЯ B.2, NB7 ~) ---

ПРОМПТ:
  Реализуй algorithms/subclass_growth.py — канонический шаг B.2.

АЛГОРИТМ (КАНОН):
  Вход: X, initial_pairs (S×2) из CosineSecondVectorAttacher
  Y_bases[s] = X[pairs[s]].T   # (N, 2) каждый
  labels[all paired indices] = assigned
  remaining = unassigned indices
  while remaining:
      build R_matrix shape (len(remaining), S):
          R_matrix[r,s] = conjugate_criterion(X[remaining[r]], Y_bases[s])
      (r*, s*) = argmax R_matrix
      assign remaining[r*] to s*, append vector to Y_bases[s*]  # basis grows
      remove from remaining
  return labels_, Y_bases

  Опции:
    freeze_basis_at: int | None = 2
      если 2 — после fit оставить только первые 2 столбца Y_s для export/classifier
    strategy: "default" | "master"
      master (NB7): ratio R(x,s)/mean(R) при отборе представителей

API:
  class ConjugacyClusterGrowth:
      def fit(self, X, pairs) -> tuple[np.ndarray, list[np.ndarray]]: ...

КРИТЕРИИ:
  - Ровно один вектор assign за итерацию while
  - Все M векторов получают label
  - theory test: на synthetic кластерах sanity check

ИСТОЧНИК: теория B.2; NB7 — reference (strategy master)

--- ФАЗА 3.5: FursovClusterer — фасад (ТЕОРИЯ A+B) ---

ПРОМПТ:
  algorithms/fursov_clusterer.py + refactor models/clusterer.py.

API:
  class FursovClusterer:
      def __init__(self, n_subclasses=8, reg_param=1e-8,
                   freeze_basis_at=2, growth_strategy="default"): ...
      def fit(self, X) -> "FursovClusterer":
          # A.1 GlobalMinCosinePairFinder
          # A.2–A.3 ReferenceCenterBuilder
          # B.1 CosineSecondVectorAttacher
          # B.2 ConjugacyClusterGrowth
      @property
      def subspaces_(self) -> list[np.ndarray]:  # S matrices (N, 2)
      @property
      def labels_(self) -> np.ndarray: ...

  SubspaceClusterer = FursovClusterer  # alias или thin wrapper

КРИТЕРИИ:
  - fit() на (M=100, N=512) synthetic завершается без ошибок
  - len(subspaces_) == n_subclasses, each Y.shape[1] == freeze_basis_at

--- ФАЗА 3.6: SubclassExport + Legacy notebook pipeline ---

ПРОМПТ:
  algorithms/subclass_export.py — экспорт Y_s в формат 8_{class}_subclasses_vectors.csv
  algorithms/legacy/notebook_pipeline.py — staged NB4→NB5→NB6→NB7 для parity

--- ФАЗА 3.7: Parity & theory tests ---

ПРОМПТ:
  tests/test_theory/test_clustering_theory.py — synthetic, проверка A.1, A.2, B.1, B.2
  tests/test_parity/test_glioma_notebook_pipeline.py — CSV ноутбуков (skipif no data)

--------------------------------------------------------------------------------
ФАЗА 4 — CLASSIFIER & EVALUATION (ТЕОРИЯ C, NB8)
Приоритет: P1 | Срок: 2 дня | Зависит от: фаза 3.5–3.6
--------------------------------------------------------------------------------

ПРОМПТ:
  Расширь classifier.py и evaluation/metrics.py по теории C и NB8.

АЛГОРИТМ (КАНОН = NB8):
  24 базиса Y_s (N×2), flat argmax R, subclass→class mapping.
  proportion = best_R / mean(other_R) - 1

API (дополнения):
  fit_from_subclass_bases(subspaces_by_class)
  predict_subclass(X) → 0..23
  predict_r_matrix_flat(X) → (M, 24)
  predict_confidence_ratio(X)

КРИТЕРИИ:
  - accuracy >= 0.70 на test75 (notebook parity, допуск ±0.02 от 0.72).
    ⚠ См. находку №2 в разделе 0: оригинального test75 в репозитории нет,
    буквально проверить нельзя. tests/test_parity/test_classifier_real_data.py
    проверяет ослабленный, но честный критерий — accuracy значимо выше
    случайного угадывания на собственном holdout-сплите реальных МРТ.

ИСТОЧНИК: теория C, NB8

--------------------------------------------------------------------------------
ФАЗА 5 — IO & PERSISTENCE
Приоритет: P2 | Срок: 1 день
--------------------------------------------------------------------------------

Дополнить io/vectors.py:

  | CSV ноутбука                         | Функция                        | Канон/Legacy |
  |--------------------------------------|--------------------------------|--------------|
  | {class}_horizontal_vector.csv        | load/save_vectors_csv          | NB3          |
  | {class}_class_first_and_second_*.csv | load_initial_pair_indices      | Legacy NB4–5 |
  | {class}_class_core_vectors.csv       | load_center_indices            | A.2–A.3      |
  | {class}_new_classes.csv              | load_subclass_pairs            | B.1 ~ NB6    |
  | 8_{class}_subclasses_vectors.csv     | load_subclass_bases            | Export       |

  save_pipeline_artifact / load_pretrained_classifier

--------------------------------------------------------------------------------
ФАЗА 6 — PIPELINE ORCHESTRATOR
Приоритет: P2 | Срок: 2 дня
--------------------------------------------------------------------------------

FursovPipeline.run_stage():

  | stage               | Теория    | Модуль                  | Ноутбук |
  |---------------------|-----------|-------------------------|---------|
  | resize              | —         | preprocessing           | NB1     |
  | center              | —         | preprocessing           | NB2     |
  | vectorize           | вход X    | features                | NB3     |
  | global_pair         | A.1       | global_pair             | —       |
  | reference_centers   | A.2–A.3   | reference_centers       | NB5     |
  | subclass_seed       | B.1       | subclass_seed           | NB6     |
  | subclass_growth     | B.2       | subclass_growth         | NB7     |
  | cluster             | A+B       | fursov_clusterer        | —       |
  | export_subspaces    | export    | subclass_export         | NB6–7   |
  | classify            | C         | classifier              | NB8     |
  | legacy_notebook     | —         | legacy/notebook_pipeline| NB4–7   |

  run_class(class_name): vectorize → cluster (канон) → export_subspaces
  run_all_classes(): для glioma, meningioma, pituitary
  classify_test(): accuracy metrics

--------------------------------------------------------------------------------
ФАЗА 7 — THIN NOTEBOOK WRAPPERS
Приоритет: P3 | Срок: 1 день
--------------------------------------------------------------------------------

  config = DatasetConfig(root=r"C:\mission_6states\data")
  pipeline = FursovPipeline(config)

  | Ноутбук | Рекомендуемый вызов                          | Примечание        |
  |---------|----------------------------------------------|-------------------|
  | 1       | pipeline.run_stage("resize")                 |                   |
  | 2       | pipeline.run_stage("center")                 |                   |
  | 3       | pipeline.run_stage("vectorize")            |                   |
  | 4       | pipeline.run_stage("legacy_notebook", step=4)| parity, не канон  |
  | 5       | run_stage("reference_centers") или legacy    |                   |
  | 6       | run_stage("subclass_seed") или legacy        |                   |
  | 7       | run_stage("subclass_growth") или legacy      |                   |
  | 8       | pipeline.run_stage("classify")               | теория C          |

  Новые thin notebooks могут вызывать pipeline.run_stage("cluster") — канон A+B.

--------------------------------------------------------------------------------
ФАЗА 8 — ТЕСТЫ & PARITY
Приоритет: P3 | Срок: 3–4 дня
--------------------------------------------------------------------------------

Два слоя тестов:

  @pytest.mark.theory (канон, synthetic):
    - A.1: global pair = argmin всех пар
    - A.2: 3-й центр = argmin R против Y(N×2)
    - A.3: k-й центр использует Y из (k-1) центров
    - B.1: каждый центр + min-cos второй вектор
    - B.2: sequential global argmax R, one vector per step
    - C: flat 24-way argmax

  @pytest.mark.notebook_parity (CSV, skipif no dataset):
    - legacy NB4 trio_list
    - NB5 center indices (ожидать расхождение если pair ≠ global)
    - NB6/NB7 CSV
    - NB8 accuracy ≥ 0.70 (см. находку №2 в разделе 0 — на практике
      проверяется как "> chance", буквального test75 в репозитории нет)

  Property: R ∈ [0,1], cosine diagonal = 1

  Coverage target: ≥ 70%

================================================================================
5. РЕКОМЕНДУЕМЫЙ ПОРЯДОК ВЫПОЛНЕНИЯ
================================================================================

  0 (infra)
    ↓
  2 (vectorization + io/vectors)
    ↓
  3.1 global_pair (теория A.1)
    ↓
  3.2 reference_centers (A.2–A.3)
    ↓
  3.3 subclass_seed (B.1)
    ↓
  3.4 subclass_growth (B.2)
    ↓
  3.5 fursov_clusterer (фасад)
    ↓
  3.6 export + legacy/notebook_pipeline
    ↓
  4 (classifier — теория C)
    ↓
  8 theory tests (synthetic)          ← подтверждение канона
    ↓
  8 parity tests (CSV ноутбуков)        ← legacy совместимость
    ↓
  1 (preprocessing)
    ↓
  5 (io extensions)
    ↓
  6 (pipeline orchestrator)
    ↓
  7 (thin notebooks)

================================================================================
6. ТЕХНИЧЕСКИЙ ДОЛГ — ИСПРАВИТЬ ПРИ РЕФАКТОРИНГЕ
================================================================================

  1. Канон (теория) — единственный путь в FursovClusterer и FursovPipeline.run_class().
     Legacy NB4–7 — только через run_stage("legacy_notebook") и test_parity.

  2. Убрать duplicate conjugate_criterion в clusterer.py / classifier.py.

  3. Не воспроизводить баг NB4 (vector_norm(first_reference)).

  4. int(norm) в ноутбуках → float64 в библиотеке.

  5. reg_param через config/constructor.

  6. logging вместо print. ✅ Сделано (2026-09-19): logging.getLogger(__name__)
     во всех модулях с реальной логикой (algorithms/*, core/metrics.py,
     models/*, io/*, features/*, preprocessing/*, pipeline/*,
     evaluation/metrics.py, utils/validation.py, config/dataset.py);
     NullHandler на пакетном логгере + subspace_conjugacy.configure_logging()
     как опциональный helper для консольного вывода (utils/logging_config.py).

  7. Type hints Python 3.10+.

  8. NumPy docstrings.

  9. freeze_basis_at=2 для export в classifier (теория C требует k=2).

  10. Документировать в docstring каждого algorithm-модуля ссылку на шаг теории
      (A.1, A.2, B.1, ...).

================================================================================
7. ОЦЕНКА СРОКОВ
================================================================================

  | Фаза | Описание                         | Приоритет | Срок     |
  |------|----------------------------------|-----------|----------|
  | 0    | Инфраструктура                   | P0        | 1–2 дня  |
  | 1    | Preprocessing                    | P2        | 2–3 дня  |
  | 2    | Vectorization + CSV IO           | P0        | 1–2 дня  |
  | 3    | Algorithms (теория A–B + legacy) | P1        | 5–6 дней |
  | 4    | Classifier (теория C)            | P1        | 2 дня    |
  | 5    | IO extensions                    | P2        | 1 день   |
  | 6    | Pipeline orchestrator            | P2        | 2 дня    |
  | 7    | Thin notebooks                   | P3        | 1 день   |
  | 8    | Theory + parity tests            | P3        | 3–4 дня  |

  ИТОГО: ~3–4 недели.

================================================================================
8. ШАБЛОН ПРОМПТА ДЛЯ СЕССИИ
================================================================================

  Ты Senior Python developer. Проект: c:\mission_6states.
  Библиотека: subspace_conjugacy. План: refactoring_plan.txt, ФАЗА N.

  КАНОН: секция «1. THEORY ALIGNMENT» — source of truth для алгоритмов
  кластеризации. Legacy ноутбуки — только для parity (pytest.mark.notebook_parity).

  Задача: [описание из секции фазы N].

  Требования:
  - Реализуй КАНОН из секции 1, если фаза помечена теорией A/B/C
  - Legacy код — только в algorithms/legacy/
  - conjugate_criterion, cosine_similarity_matrix из core.metrics
  - DatasetConfig для путей
  - pytest: @pytest.mark.theory для канона
  - Критерии приёмки — из refactoring_plan.txt

  Исходные ноутбуки (reference): [список].
  Не меняй unrelated code.

================================================================================
9. ПОИСК ГИПЕРПАРАМЕТРОВ (добавлено 2026-09-20)
================================================================================

subspace_conjugacy/model_selection/ — grid search и random search для
SubspaceConjugacyClassifier поверх sklearn.model_selection (не собственная
реализация перебора — классификатор уже sklearn-эстиматор, GridSearchCV/
RandomizedSearchCV работают "из коробки"):
  - param_space.py    — DEFAULT_PARAM_GRID / DEFAULT_PARAM_DISTRIBUTIONS
                         (n_subclasses, growth_strategy, reg_param;
                         freeze_basis_at сознательно не варьируется — теория
                         Фазы C требует k=2, это не тюнинг-параметр)
  - search.py          — grid_search_classifier, random_search_classifier,
                         search_hyperparameters (диспетчер), summarize_search_results
  Экспортированы из subspace_conjugacy напрямую (top-level API).

НАХОДКА №4 (критический баг совместимости со sklearn, исправлен при
добавлении этого модуля): SubspaceConjugacyClassifier был объявлен как
``class SubspaceConjugacyClassifier(BaseSubspaceEstimator, ClassifierMixin)``.
В современных версиях sklearn (тегирование эстиматора через
__sklearn_tags__) такой порядок баз приводит к тому, что
is_classifier(clf) == False — BaseEstimator в MRO разрешается раньше
ClassifierMixin и перекрывает тег "classifier". Практическое следствие:
GridSearchCV/RandomizedSearchCV/cross_val_score с целочисленным ``cv``
использовали обычный KFold вместо StratifiedKFold; на данных, где объекты
идут блоками по классам (типичная раскладка MRI-датасета до shuffle),
train-фолд целиком терял один класс, и accuracy на всех фолдах была РОВНО
0.0 — без единого исключения, то есть баг был полностью незаметен снаружи.
Исправлено обращением порядка: ``class SubspaceConjugacyClassifier(ClassifierMixin,
BaseSubspaceEstimator)`` (mixin — первым, стандартная рекомендация sklearn
для множественного наследования). Регрессия закрыта тестом
tests/test_model_selection.py::TestIsClassifierRegression.

================================================================================
10. СВЕРКА С ОПУБЛИКОВАННОЙ СТАТЬЁЙ (Korshikov & Fursov, добавлено 2026-09-20)
================================================================================

theory/VI_Korshikov_VA_Fursov_Pathology_Recognition_Based_on_Conjugacy.docx —
опубликованная статья с тем же методом + экспериментальная валидация на
Kaggle Brain Tumor MRI датасетах. Формулы (1)-(3) (R(x,Y), cosine, растущая
матрица центров) подтверждают канон из раздела 1 буквально. Найденные при
сверке расхождения библиотеки со статьёй (по приоритету):

  №1 n_subclasses — единый int на все классы, а не per-class. ✅ ИСПРАВЛЕНО
     2026-09-20: SubspaceConjugacyClassifier(n_subclasses=...) теперь
     принимает int (как раньше, без изменения поведения) ИЛИ
     Mapping[ClassLabel, int] — независимое число подклассов на класс
     (статья, "Description of the Clustering Method": оптимальное число
     подклассов обычно различается между патологиями; Table I — glioma/
     meningioma/pituitary достигают максимума accuracy при разном числе).
     При словаре: обязателен ключ для каждого класса из y при fit()
     (иначе ValueError со списком недостающих), лишние ключи — WARNING и
     игнорируются. Фактически применённые значения — в новом атрибуте
     self.n_subclasses_by_class_ (Dict[ClassLabel, int], заполняется и при
     fit(), и при fit_from_subclass_bases() — во втором случае выводится из
     len(subspaces_by_class[cls])). BaseSubspaceEstimator._validate_input_params
     обобщён на Mapping. FursovPipeline.run_all_classes(n_subclasses=...)
     получил тот же dict-fan-out (для симметрии с классификатором) — при
     Mapping распределяет разные значения по run_class() для каждого
     класса, при отсутствии значения для класса — ValueError.
     grid_search_classifier/random_search_classifier не требовали изменений:
     dict — обычное значение точки param_grid для sklearn (set_params
     принимает любой Python-объект), см. tests/test_model_selection.py::
     TestGridSearchClassifier::test_can_grid_search_over_per_class_n_subclasses_dicts.
     Тесты: tests/test_classifier.py::TestPerClassNSubclasses,
     tests/test_pipeline.py::TestRunAllClasses (dict-варианты).

  №2 freeze_basis_at=2 отбрасывает результат роста подпространства (B.2) —
     классификатору достаётся только исходная пара из B.1, а не фактически
     выросший базис. Статья растит подпространства до реального размера и
     срезает до размера НАИМЕНЬШЕГО подпространства между классами только
     для честного сравнения R(x,Y) — не фиксирует k=2 заранее.
     ✅ ИСПРАВЛЕНО 2026-09-20:
       - algorithms/subclass_export.py::equalize_subspace_bases(subspaces_by_class)
         — новая функция, буквальный рецепт статьи ("only the first n
         elements corresponding to the number of vectors of the smallest
         space are taken for each vector"): усекает базисы ВСЕХ подклассов
         ВСЕХ классов до общего минимального фактически достигнутого k.
       - SubspaceConjugacyClassifier(freeze_basis_at=...) принимает, кроме
         int (старое поведение, дефолт не менялся — 2) и None (полный рост
         БЕЗ равнения между классами, как раньше у FursovClusterer), новое
         значение "auto": каждый класс растится без ограничения
         (freeze_basis_at=None внутри FursovClusterer — рост и так ВСЕГДА
         идёт до конца независимо от freeze_basis_at, freeze_basis_at лишь
         решает, что оставить на выходе, так что "auto" не стоит ничего
         сверх текущей стоимости fit()), затем equalize_subspace_bases
         равняет всё к общему минимуму. Итоговое k — в новом атрибуте
         self.equalized_basis_size_ (None, если equalize не применялся).
       - fit_from_subclass_bases(subspaces_by_class, equalize=False) — тот
         же механизм для уже готовых базисов (например, полученных вне
         fit(), с разным k у разных классов).
       - FursovPipeline.build_classifier(equalize=False) — форвардит в
         fit_from_subclass_bases(equalize=...), для сценария
         run_all_classes(freeze_basis_at=None) + сборка классификатора.
       - predict_r_matrix()/predict_r_matrix_flat() задокументировали явно:
         R(x,Y) не масштабируется по k, сравнение между классами честно
         только при равном k у всех подпространств.
       ⚠ Дефолт freeze_basis_at остаётся int=2 (обратная совместимость,
       поведение/точность существующих пользователей не меняется молча) —
       "auto" нужно включать явно. Тесты:
       tests/test_algorithms/test_subclass_export.py::TestEqualizeSubspaceBases,
       tests/test_classifier.py::TestFreezeBasisAtAuto,
       tests/test_classifier.py::TestFitFromSubclassBasesEqualize,
       tests/test_pipeline.py::TestBuildClassifier (equalize-варианты).

  №3 Growth-цикл исчерпывающий (все векторы обязательно куда-то
     присоединяются); статья допускает, что часть эталонных векторов
     останется неприсоединённой, и отдельно исключает почти линейно
     зависимые векторы ещё до кластеризации (фильтра нет в библиотеке).
     ✅ ИСПРАВЛЕНО 2026-09-20 (переинтерпретация): при повторном чтении
     статьи ("Description of the Clustering Method": "so on until the
     distribution of all free vectors is completed... It is possible that
     some number of reference vectors will remain unallocated") наиболее
     естественное прочтение — "неприсоединённые векторы" — это именно те,
     что исключены на этапе "Problem Definition" ("almost linearly
     dependent vectors are excluded from the set of reference vectors")
     ДО начала кластеризации, а не результат того, что growth-цикл
     останавливается раньше срока по неизвестному критерию (статья не
     приводит формулы/условия для такой ранней остановки — придумывать её
     произвольно означало бы менять уже протестированный канон B.2 без
     обоснования). Поэтому:
       - Growth-цикл (ConjugacyClusterGrowth/B.2) НЕ изменён — остаётся
         исчерпывающим по своим собственным правилам, как и раньше.
       - Добавлен только фильтр почти линейно зависимых эталонных векторов
         (буквально "Problem Definition"):
         algorithms/reference_filter.py::LinearDependencyFilter — проходит
         по векторам по порядку, накапливает "принятое" подпространство
         Y_accepted (первый вектор — безусловно), последующий вектор
         исключается, если R(x, Y_accepted) >= threshold (по умолчанию
         0.999) — т.е. почти целиком объясняется уже принятыми (тот же
         criterion сопряжённости R(x,Y) из core/metrics.py: R->1 означает
         "x почти линейно зависим от столбцов Y" — формальное определение).
       - FursovClusterer(filter_dependent=False (default), dependency_threshold=0.999):
         при True прогоняет фильтр ПЕРЕД фазой A.1; исключённые векторы
         никогда не участвуют в A.1-B.2 и получают label=-1 в
         self.labels_ (длина labels_ всегда равна исходному M, индексация
         всех публичных атрибутов center_indices_/initial_pairs_/
         get_initial_pair()/kept_indices_/excluded_indices_ — в
         пространстве индексов ИСХОДНОГО X, переданного в fit(), с явным
         remapping из локального индексного пространства отфильтрованного
         подмножества).
       - SubspaceConjugacyClassifier(filter_dependent=False (default),
         dependency_threshold=0.999) — тот же флаг прокидывается в
         FursovClusterer НЕЗАВИСИМО для каждого класса; новый атрибут
         excluded_indices_by_class_ (Dict[ClassLabel, np.ndarray],
         индексы — в исходном X глобально, не в подвыборке класса).
       - Совместимо с freeze_basis_at="auto"/equalize (находка №2) —
         независимые оси, компонуются без конфликтов.
       ⚠ Важное математическое свойство (задокументировано в Notes
       LinearDependencyFilter): как только число принятых векторов
       достигает размерности признаков N, ЛЮБОЙ следующий вектор
       неизбежно почти линейно зависим (полный ранг исчерпан) — для
       реальных МРТ (N=65536, M~сотни) это не проявляется, но легко
       спутать с багом на низкоразмерных синтетических тестах (M > N).
       Дефолт filter_dependent=False — обратная совместимость.
       Тесты: tests/test_algorithms/test_reference_filter.py,
       tests/test_algorithms/test_fursov_clusterer.py::TestFursovClustererDependencyFilter,
       tests/test_classifier.py::TestFilterDependent.

  №4 Целый первый этап пайплайна статьи (Otsu-бинаризация + классификация
     проекции axial/sagittal/coronal перед классификацией типа опухоли) не
     реализован. Архитектурно тривиально (SubspaceConjugacyClassifier
     универсален), но нужны: Otsu-препроцессинг, разметка по проекциям,
     обёртка для двух последовательных классификаторов.
     ✅ ИСПРАВЛЕНО 2026-09-20 (примитивы + оркестрация; готовых
     проекционно-размеченных данных в репозитории всё равно нет — реальный
     сквозной прогон на Kaggle-датасете статьи пользователь собирает сам,
     see примеры ниже):
       - preprocessing/binarization.py::otsu_threshold/otsu_binarize —
         тонкая обёртка над cv2.THRESH_OTSU (формула (2) статьи — обычный
         метод Отсу, переизобретать не нужно, как и с LANCZOS в resize.py).
         invert=False по умолчанию (полезные пиксели белые, фон чёрный —
         как в статье). batch-вариант binarize_directory (тот же контракт,
         что resize_directory/center_directory: вход/выход-директория,
         glob pattern). Применяется ТОЛЬКО к этапу проекции — статья
         явно: "for the task of tumor type detection... binarization is
         not performed".
       - DatasetConfig.paths[cls]["binarized"] — новый путь (root/4_{cls}_
         binarized), НЕ входит в default stages create_directories() (это
         опциональная стадия, не часть обычного tumor-type пайплайна).
       - pipeline/stages.py::stage_binarize + STAGE_REGISTRY["binarize"] —
         centered -> binarized, вызывается явно через run_stage("binarize",
         ...), не входит в run_class()/run_all_classes().
       - models/sequential_classifier.py::SequentialClassifier — универсальная
         (не привязана к SubspaceConjugacyClassifier) композиция
         classifier этапа 1 + Dict[label, classifier] этапа 2:
         fit(X1, y1, X2_by_group, y2_by_group) обучает каждый классификатор
         на СВОИХ данных (как в статье — разные датасеты для проекции и
         для типа опухоли, не общий датасет с двумя параллельными
         метками); predict(X1, X2) -> (stage1_pred, stage2_pred), где
         этап 2 применяется к классификатору, соответствующему
         ПРЕДСКАЗАННОЙ метке этапа 1 — буквально "the result obtained at
         the previous stage of classification becomes a set of data for
         the next stage". predict_stage2(X2, labels) отдельно принимает
         либо предсказанные, либо истинные метки этапа 1 (для изолированной
         оценки качества этапа 2). is_fitted_ — вычисляемое свойство
         (True сразу, если переданы уже обученные классификаторы).
       Тесты: tests/test_preprocessing.py::TestOtsuBinarization/
       TestBinarizeDirectory, tests/test_pipeline.py::TestRunStageBinarize,
       tests/test_sequential_classifier.py, tests/test_config.py (путь
       "binarized").

  №5 Фильтр малоинформативных изображений (доля белых пикселей < 50% от
     средней по выборке, статья: 3-й эксперимент) — измеримо поднимает
     accuracy в статье, в библиотеке отсутствует.
     ✅ ИСПРАВЛЕНО 2026-09-20 (та же архитектура, что и у LinearDependencyFilter
     из находки №3 — fit/transform, kept_indices_/excluded_indices_,
     remapping в пространство исходного X):
       - algorithms/informativeness_filter.py::LowInformativenessFilter —
         буквальная реализация цитаты статьи: "images with the number of
         white pixels less than 50% of the average number of white pixels
         in the images of the set are cut off". Работает на уже
         ВЕКТОРИЗОВАННЫХ данных (подсчёт "белых" элементов вектора вместо
         пикселей изображения — эквивалентно, порядок обхода при
         векторизации не важен для суммы), поэтому умещается в тот же слой,
         что и LinearDependencyFilter, а не в preprocessing/. "Белый"
         элемент — >= brightness_threshold (default=10, согласовано с
         preprocessing.normalization.DEFAULT_THRESHOLD). Порог отсечения =
         min_fraction_of_mean (default=0.5, буквально "50%") * среднее
         число белых элементов по ВСЕЙ переданной выборке.
       - FursovClusterer(filter_low_informativeness=False (default),
         informativeness_threshold=10, informativeness_min_fraction=0.5) —
         применяется КАК ФАЗА 0a, ПЕРЕД LinearDependencyFilter (фаза 0b,
         находка №3): сначала отбрасываем малоинформативные образы
         (проблема качества данных), затем — почти дублирующие среди
         оставшихся (проблема избыточности данных). Если оба фильтра
         включены — LinearDependencyFilter.fit() вызывается уже на
         survivors фазы 0a, а не на полном X. excluded_indices_ теперь —
         np.union1d двух отдельных атрибутов excluded_by_informativeness_ и
         excluded_by_dependency_ (оба всегда заполнены — пустым массивом,
         если соответствующий фильтр выключен), в пространстве индексов
         ИСХОДНОГО X, как и раньше для excluded_indices_/kept_indices_/
         center_indices_/initial_pairs_.
       - SubspaceConjugacyClassifier(filter_low_informativeness=False
         (default), informativeness_threshold=10,
         informativeness_min_fraction=0.5) — прокидывается в
         FursovClusterer НЕЗАВИСИМО для каждого класса (среднее число
         "белых" элементов считается по объектам ЭТОГО класса, не по всей
         выборке — согласовано с тем, как n_subclasses/filter_dependent уже
         применяются независимо на класс). excluded_indices_by_class_
         теперь заполняется, если ЛЮБОЙ из двух фильтров включён (было:
         только filter_dependent) — семантика не изменилась для тех, кто
         не включал filter_dependent (по умолчанию оба фильтра выключены,
         атрибут остаётся None).
       Дефолты обоих новых параметров — выключено, обратная совместимость
       не нарушена.
       Тесты: tests/test_algorithms/test_informativeness_filter.py,
       tests/test_algorithms/test_fursov_clusterer.py::
       TestFursovClustererInformativenessFilter/TestFursovClustererCombinedFilters,
       tests/test_classifier.py::TestFilterLowInformativeness.

  №6 Датасет/сплит статьи (300/375/450 изображений, 80/20) отличается от
     жёстких 100 train + 75 test исходных ноутбуков — не баг, контекст:
     текущие дефолты (n_subclasses=8 и т.п.) унаследованы от ноутбуков, а
     не выведены из экспериментов статьи.

================================================================================
11. НОВЫЙ МЕТОД ИЗ ЧЕРНОВИКА (theory/Макет новой статьи.docx, добавлено 2026-09-20)
================================================================================

theory/Макет новой статьи.docx — ЧЕРНОВИК ДРУГОЙ, НЕ опубликованной статьи
("Детерминированный подход к формированию обучающих подпространств для
распознавания медицинских изображений", Коршиков В.И., Фурсов В.А.), про
классификацию КТ-изображений грудной клетки (не МРТ мозга). В отличие от
раздела 10 (сверка с уже ОПУБЛИКОВАННОЙ статьёй про МРТ), это неоконченный
черновик — Введение и раздел про нейросети в нём не дописаны (заготовки
"здесь надо сделать"), метод не прошёл рецензирование.

Черновик описывает двухэтапную технологию отбора эталонных векторов:
  - "Этап 2" черновика = уже реализованный канон (раздел 1 этого документа,
    фазы A.1-A.3 + решающее правило фазы C) — совпадает буквально.
  - "Этап 1" черновика — НОВЫЙ алгоритм, до этого добавления отсутствовавший
    в библиотеке: "Для произвольно заданного начального вектора... на этом
    множестве определяется вектор... для которого нормированный коэффициент
    корреляции [максимален]... Каждый из этой пары векторов заносится в
    своё подмножество (A, B). Процедура повторяется на множестве оставшихся
    доступных векторов до их исчерпания. В результате... множество будет
    разделено на два подмножества (A, B) похожих векторов. Любое из этих
    подмножеств может использоваться для формирования обучающего
    подпространства данного класса."

  ✅ РЕАЛИЗОВАНО 2026-09-20:
    - algorithms/correlated_pair_splitter.py::CorrelatedPairSplitter —
      итеративно (в порядке возрастания ещё не использованного исходного
      индекса) находит для очередного вектора наиболее похожий (МАКСИМУМ
      cosine_similarity_matrix из core/metrics.py — та же метрика, что и в
      A.1, но ищется похожесть, а не непохожесть) среди оставшихся, делит
      пару между двумя подмножествами (subset_a_indices_/subset_b_indices_),
      убирает оба из рассмотрения. При нечётном M один вектор остаётся без
      пары (unpaired_index_), не входит ни в одно подмножество.
    - Отличие от LinearDependencyFilter (находка №3, раздел 10): та
      использует последовательное накопление подпространства Y_accepted и
      ПОРОГ R(x, Y_accepted) для одностороннего исключения; этот алгоритм —
      попарное сопоставление БЕЗ порога, с разбиением пополам, без
      отбрасывания информации о содержимом класса (оба подмножества
      покрывают исходное множество).
    - FursovClusterer(split_correlated_pairs=False (default),
      correlated_pairs_subset="a") — новая ФАЗА 0c, выполняется ПОСЛЕ 0a
      (LowInformativenessFilter) и 0b (LinearDependencyFilter), ПЕРЕД
      фазой A.1, на том, что осталось после 0a/0b. Отброшенное подмножество
      (+ непарный вектор, если есть) попадает в новый атрибут
      excluded_by_correlation_split_ и в общий excluded_indices_/labels_==-1
      — тот же контракт, что и у существующих фильтров.
    - SubspaceConjugacyClassifier(split_correlated_pairs=False (default),
      correlated_pairs_subset="a") — прокидывается в FursovClusterer
      НЕЗАВИСИМО для каждого класса, как и filter_dependent/
      filter_low_informativeness; excluded_indices_by_class_ заполняется,
      если включён ЛЮБОЙ из трёх фильтров/сплиттеров.
    - Экспортировано в algorithms/__init__.py и subspace_conjugacy/__init__.py.

  Неоднозначности черновика и принятые решения (см. также docstring модуля):
    - Формула (1) черновика ("нормированный коэффициент корреляции") не
      восстановилась при извлечении текста из .docx (встроенный объект
      формулы) — по терминологии, совпадающей с формулой (3) опубликованной
      статьи ("normalized correlation coefficient" = cosine similarity) и с
      документом "Кластеризация по критерию сопряжённости.docx" (тот же
      "косинусный критерий" на первом шаге), принято: это cosine similarity.
    - "Произвольно заданный начальный вектор" — детерминирован как
      наименьший ещё не использованный исходный индекс (воспроизводимость).
    - "m чётно" — при нечётном M библиотека не приписывает непарный вектор
      произвольно ни к одному из подмножеств, а явно исключает его
      (unpaired_index_ / excluded_by_correlation_split_).
    - По умолчанию ВЫКЛЮЧЕНО (в отличие от степени доверия к находкам
      раздела 10 — там источник опубликован и проверен экспериментально;
      здесь источник — незавершённый черновик без собственных
      экспериментальных результатов для этого алгоритма).

  Тесты: tests/test_algorithms/test_correlated_pair_splitter.py,
  tests/test_algorithms/test_fursov_clusterer.py::
  TestFursovClustererCorrelatedSplit,
  tests/test_classifier.py::TestSplitCorrelatedPairs.

  ЭКСПЕРИМЕНТ (main.py --experiment draft-method, добавлено 2026-09-20,
  расширено 2026-09-20): datasets/{glioma,meningioma,pituitary}_centered/
  (200 PNG/класс, 80/20 train/test, seed=42, один и тот же тестовый сплит для
  всех подходов ниже) — сравнение расширенной сетки SubspaceConjugacyClassifier
  (12 конфигураций: baseline, отдельно filter_dependent/filter_low_
  informativeness (находки №3/№5), метод из черновика (подмножества A/B) в
  комбинации с обоими фильтрами статьи, с growth_strategy="master" (NB7), с
  freeze_basis_at="auto" (находка №2) и перебор n_subclasses ∈ {4,6,8,12,16}),
  классических ML-моделей sklearn (LogisticRegression/LinearSVM/RandomForest/
  kNN поверх PCA-100 на тех же 65536-мерных векторах) и двух НАМЕРЕННО
  упрощённых CNN (PyTorch/CPU, TinyCNN 6.4K и SmallCNN 29.7K параметров,
  64x64, 30 эпох). Полные параметры и confusion matrix каждой конфигурации —
  artifacts/draft_method_experiment_report.json.

  Результаты (mean accuracy по 3 классам, отсортировано по убыванию):

    SmallCNN (CNN, 29.7K парам.)                      0.908
    LogisticRegression (классич. ML, PCA-100)         0.892
    LinearSVM / RandomForest (классич. ML, PCA-100)   0.875
    kNN k=5 (классич. ML, PCA-100)                    0.867
    TinyCNN (CNN, 6.4K парам.)                        0.842
    Сопряжённость + черновик (B), n_subclasses=16     0.800  (240 векторов, 31s)
    Сопряжённость + черновик (B), n_subclasses=12     0.775  (240 векторов, 38s)
    Сопряжённость + черновик (B) [+ разные варианты]  0.725-0.742 (240 векторов, ~45-55s)
    Сопряжённость + черновик, подмножество A          0.658  (240 векторов, 48s)
    Сопряжённость baseline / +filter_dependent/
      +filter_low_informativeness (без черновика)     0.567-0.575 (480 векторов, 425-451s)

  Выводы (без подгонки под ожидания theory/ — расхождения зафиксированы честно):

    1. Метод из черновика (CorrelatedPairSplitter) ВНУТРИ подпространственного
       подхода работает: поднимает mean accuracy с 0.567 (baseline) до
       0.725-0.800 в зависимости от n_subclasses, ИСПОЛЬЗУЯ ВДВОЕ МЕНЬШЕ
       эталонных векторов и в ~10-14 РАЗ БЫСТРЕЕ (31-55s против 425-451s) —
       самый однозначный положительный результат эксперимента. Особенно
       заметно на систематически слабом месте канона — recall meningioma
       вырос с 0.10-0.125 (baseline) до 0.45-0.50.
    2. Отдельные фильтры статьи (filter_dependent, filter_low_informativeness)
       НА ЭТОМ ДАТАСЕТЕ дают пренебрежимо малый эффект (0.567->0.567/0.575) —
       они убирают лишь единицы почти-дублирующихся/малоинформативных
       векторов из 160 на класс (реальные МРТ редко бывают почти линейно
       зависимыми или почти чёрными), в отличие от статьи, где эффект
       измерялся на другом датасете/сплите (300-450 изображений, Kaggle
       archive, находка №5 в этом же разделе даёт больший эффект именно там —
       см. main.py --experiment article). Само по себе НЕ противоречит
       статье — датасеты и объёмы выборок разные.
    3. Перебор n_subclasses (совместно с методом из черновика) даёт
       наибольший буст среди ВСЕХ вариантов подпространственного метода
       (0.800 при n=16, лучше, чем комбинации с другими фильтрами/
       стратегиями при n=8) — согласуется с находкой статьи, что оптимальное
       число подклассов нужно подбирать отдельно, не фиксировать априори (см.
       раздел 10, находка №1).
    4. Классический ML (LogisticRegression и т.д., поверх PCA) и особенно
       CNN — ДАЖЕ НАМЕРЕННО УПРОЩЁННЫЕ (SmallCNN — 29.7K параметров, на 4
       порядка меньше первой версии эксперимента) — на ЭТОМ КОНКРЕТНОМ
       датасете (600 изображений, один train/test сплит, без подбора
       гиперпараметров ни для одной из сторон) ПРЕВОСХОДЯТ ЛУЧШУЮ
       конфигурацию подпространственного метода (0.842-0.908 против 0.800).
       Это ПРЯМО РАСХОДИТСЯ с тезисом Introduction обоих документов theory/
       о том, что классические/нейросетевые подходы требуют существенно
       большей выборки, чем метод сопряжённости, — при 480 обучающих
       изображениях этого расхождения не наблюдается. Зафиксировано честно,
       без подгонки под ожидания статьи; не исключено, что на МЕНЬШИХ
       обучающих выборках (десятки, не сотни изображений на класс) картина
       была бы иной — это отдельный, не проверенный здесь эксперимент.
    5. Оговорка методологии: ни для одной из трёх сторон сравнения не
       делался полный перебор гиперпараметров (кроме n_subclasses для
       подпространственного метода) — только один train/test сплит с
       фиксированным seed=42; PCA-100 для классического ML и upstream-
       архитектуры CNN выбраны как разумные значения по умолчанию, а не как
       результат тюнинга. Для строгого вывода о превосходстве подхода
       потребовался бы перебор гиперпараметров и кросс-валидация по всем
       трём осям.

================================================================================
КОНЕЦ ДОКУМЕНТА
================================================================================
